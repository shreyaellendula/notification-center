# notification-center
A RESTful backend application built with Spring Boot that provides an in-app notification center. Implements CRUD-related notification operations using Java, Spring Data JPA, Hibernate, and MySQL.
